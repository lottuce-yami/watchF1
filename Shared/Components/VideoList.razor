@using F1Project.Data.Database.Types

@foreach(var video in List.Where(video => !string.IsNullOrWhiteSpace(video.Id)))
{
    <a href="watch?v=@video.Id">
        <img src="@video.Preview" alt="">
        @video.Title
    </a>
}

@code {
    [Parameter]
    public IEnumerable<Video> List { get; set; } = null!;
}